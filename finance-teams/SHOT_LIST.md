# Screenshot shot list

Drop PNGs into `assets/` with these exact filenames and they appear automatically
(placeholders show "coming soon" until the file exists). ~1280-1440px, light mode, demo data only.

| # | File | Module | What to capture |
|---|---|---|---|
| 1 | `assets/fin-m1-flux.png` | M1 | a variance/flux answer drilled to the driver |
| 2 | `assets/fin-m4-boardpack.png` | M4 | the generated board report with exec summary and charts |
| 3 | `assets/fin-m5-playbook.png` | M5 | the monthly flux playbook with its schedule |
| 4 | `assets/fin-m6-patch.png` | M6 | a proposed ontology patch for a finance definition |

Adding more: copy a `<figure class="shot">` block in `index.html` and set `data-todo`.
