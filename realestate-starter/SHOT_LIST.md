# Screenshot shot list

Drop PNGs into `assets/` with these exact filenames and they appear in the guide automatically.
Capture at ~1280-1440px, light mode, synthetic data only (the demo property-management set — never real tenant PII).

| # | File | Module | What to capture |
|---|---|---|---|
| 1 | `assets/re-m1-git.png` | M2 | The Git connector configured against the starter repo |
| 2 | `assets/re-m2-pr.png` | M3 | The schema-validation PR Ana opened (schema.tql diff) |

Worth adding: a property-type → sector / charge-type → recoverable rollup answer (M4), a governed occupancy or NOI answer with the basis + SQL shown (M5),
fair-housing small-cell suppression firing on a property × market × unit_type cut (M6). Copy a `<figure class="shot">` block to add slots.
