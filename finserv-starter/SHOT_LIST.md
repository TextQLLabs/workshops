# Screenshot shot list

Drop PNGs into `assets/` with these exact filenames and they appear in the guide automatically.
Capture at ~1280-1440px, light mode, demo data only (synthetic core-banking set — never customer NPI).

| # | File | Module | What to capture |
|---|---|---|---|
| 1 | `assets/fs-m1-git.png` | M1 | The Git connector configured against the starter repo |
| 2 | `assets/fs-m2-pr.png` | M2 | The schema-validation PR Ana opened (schema.tql diff) |

Worth adding: a grouper answer w/ MCC spend categories (M3), a governed dollar-weighted delinquency answer with SQL (M4),
fair-lending decline + small-population suppression firing (M5). Copy a `<figure class="shot">` block to add slots.
