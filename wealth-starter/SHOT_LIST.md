# Screenshot shot list

Drop PNGs into `assets/` with these exact filenames and they appear in the guide automatically.
Capture at ~1280-1440px, light mode, synthetic / demo data only (never real client PII).

| # | File | Module | What to capture |
|---|---|---|---|
| 1 | `assets/wm-m1-git.png` | M1 | The Git connector configured against the starter repo |
| 2 | `assets/wm-m2-pr.png` | M2 | The schema-validation PR Ana opened (schema.tql diff) |

Worth adding: an asset-class / GICS allocation answer (M3), a governed AUM or concentration answer with SQL (M4),
small-cell suppression firing on an advisor × segment cut (M5). Copy a `<figure class="shot">` block to add slots.
