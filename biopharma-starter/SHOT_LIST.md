# Screenshot shot list

Drop PNGs into `assets/` with these exact filenames and they appear in the guide automatically.
Capture at ~1280-1440px, light mode, demo data only (synthetic commercial set — never licensed Rx / real HCP).

| # | File | Module | What to capture |
|---|---|---|---|
| 1 | `assets/bp-m1-git.png` | M1 | The Git connector configured against the starter repo |
| 2 | `assets/bp-m2-pr.png` | M2 | The schema-validation PR Ana opened (schema.tql diff) |

Worth adding: a molecule / therapeutic-area rollup w/ ATC classes (M3), a governed gross-to-net answer with SQL (M4),
small-cell suppression / named-prescriber gating firing (M5). Copy a `<figure class="shot">` block to add slots.
