# Screenshot shot list

Drop PNGs into `assets/` with these exact filenames and they appear in the guide automatically.
Capture at ~1280-1440px, light mode, synthetic data only (the demo medtech set — never real customer or patient data).

| # | File | Module | What to capture |
|---|---|---|---|
| 1 | `assets/mt-m1-git.png` | M2 | The Git connector configured against the starter repo |
| 2 | `assets/mt-m2-pr.png` | M3 | The schema-validation PR Ana opened (schema.tql diff) |

Worth adding: a product family → modality / device class → FDA risk rollup answer (M4), a governed product-revenue or complaint-rate answer with the basis + SQL shown (M5),
small-cell suppression firing on an account × product-family × complaint-type cut (M6). Copy a `<figure class="shot">` block to add slots.
