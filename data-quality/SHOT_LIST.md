# Screenshot shot list

Drop PNGs into `assets/` with these exact filenames and they appear automatically
(placeholders show "coming soon" until the file exists). ~1280-1440px, light mode, demo data only.

| # | File | Module | What to capture |
|---|---|---|---|
| 1 | `assets/dq-m1-golden.png` | M1 | a golden query pinned against a trusted reference number |
| 2 | `assets/dq-m3-drift.png` | M3 | a drift alert from the scheduled validation playbook |
| 3 | `assets/dq-m5-agent.png` | M5 | the data-quality feed agent posting a load issue |

Adding more: copy a `<figure class="shot">` block in `index.html` and set `data-todo`.
