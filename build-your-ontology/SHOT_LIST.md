# Screenshot shot list

Drop PNGs into `assets/` with these exact filenames and they appear in the guide automatically.
Capture at ~1280-1440px, light mode, demo data only (Tuva/CMS demo sets — no customer data).

| # | File | Module | What to capture |
|---|---|---|---|
| 1 | `assets/byo-m4-graph.png` | M4 | The ontology graph with entities and metrics visible |

Worth adding as you run it: the schema map (M2), a rendered-SQL view (M3), the conflicting-metric
diff (M4), the two-persona side-by-side (M9). Copy a `<figure class="shot">` block to add slots.
