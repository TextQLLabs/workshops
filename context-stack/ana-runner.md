# Context Stack — Ana-Led Runner

> Pairs with `../ana-workshop-facilitator.md` (the generic HOW). This is the **module list** (the WHAT) for the Context Stack workshop.
> Delivery: **inline** (paste it) or just give Ana the workshop URL — she fetches it. Facilitation: **in-thread, interactive — the learner runs each prompt, Ana coaches.**

## Step-0 prompt (paste this, or use the workshop URL)

```
Hey Ana — facilitate the "Context Stack" workshop with me in this thread, on the data connected here.
Pull the steps from https://textqllabs.github.io/workshops/context-stack/ (or use the module list below).
Run it interactively: look at what's connected first (2–3 lines), then go ONE module at a time. For each
module, DON'T run the prompt yourself — give ME the prompt to copy and run, tell me what to look for, then
wait and coach me on my result. Start with what you see, then Module 0.
```

## Methodology (what this teaches)

A workshop of short, hands-on modules for platform owners, workspace admins, and enablement champions who configure how Ana behaves for different people. Three layers of context — organization, role, personal — composed at question time; you'll build all three, prove they work with a same-question…

## Modules — Ana adapts each `[bracket]` to the connected data, then hands the prompt to the learner to run

| # | Module | Prompt for the learner to run (resolve the brackets) |
|---|---|---|
| 0 | The Context Stack | *(Ana orients you)* — explain **The Context Stack** in 3 bullets, grounded in my connected data, then move on. |
| 1 | Prepare the Environment | Help me set up context routing for this workspace. First ask me for our org name, fiscal calendar, the metrics/terms worth governing, and our teams/personas. Then create an **ANA.md** at the root defining the three layers (ORG = everyone, ROLE = a team/persona, PERSONAL = one user) and the governance rule (personal = immediate; org/role = reviewed patch), and stub the `users/` and `behaviors/` folders. |
| 2 | Personal Context | Remember my preferences: I want concise answers with the headline number first; I work in [timezone]; default time windows to [the current quarter] unless I say otherwise; when I say "my accounts" I mean [accounts where I'm the owner]. |
| 3 | Org Context | Propose an org context patch: our official definition of [active member] is [logged in AND completed at least one action in the trailing 30 days, excluding internal and test accounts]. Save it to the org context so every user gets this definition — and open it for review. |
| 4 | Author Role Profiles | Create behaviors/[program_manager]/org_context.md and behaviors/[analyst]/org_context.md with the two persona files above, brackets adapted to our domains, and open both as patches for review — do not apply them directly. |
| 5 | The Same-Question Test | How are our [members/patients] doing on [hospital utilization] and cost? |
| 6 | Provisioning & Identity | *(Ana orients you)* — explain **Provisioning & Identity** in 3 bullets, grounded in my connected data, then move on. |
| 7 | Operate the Stack | *(Ana orients you)* — explain **Operate the Stack** in 3 bullets, grounded in my connected data, then move on. |

## When done

Recap in 3 bullets. Offer to save anything the learner *built* as a **Playbook** for reuse. Do **not** write the workshop into the governed ontology.
