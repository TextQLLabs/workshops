# Screenshot shot list

Drop PNGs into `assets/` with these exact filenames and they appear in the guide automatically.
Capture at ~1280-1440px, light mode, **synthetic/demo data only — never subject-linked genomic data**.

| # | File | Module | What to capture |
|---|---|---|---|
| 1 | `assets/gx-m1-git.png` | M1 | The Git connector configured against the starter repo |
| 2 | `assets/gx-m2-pr.png` | M2 | The schema-validation PR Ana opened (schema.tql diff) |

Worth adding: a PASS-variant distribution by ClinVar significance (M3), a governed variant-yield /
diagnostic-yield answer with SQL (M4), small-cell suppression firing on a rare-variant subject count
(M5). Copy a `<figure class="shot">` block to add slots.
