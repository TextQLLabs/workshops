# Screenshot shot list

Drop PNGs into `assets/` with these exact filenames and they appear in the guide automatically.
Capture at ~1280-1440px, light mode, synthetic data only (the demo capital-markets set — never real trading/MNPI data).

| # | File | Module | What to capture |
|---|---|---|---|
| 1 | `assets/cm-m1-git.png` | M2 | The Git connector configured against the starter repo |
| 2 | `assets/cm-m2-pr.png` | M3 | The schema-validation PR Ana opened (schema.tql diff) |

Worth adding: a super-asset-class / IG-HY-rating / lit-dark-OTC venue rollup answer (M4), a governed gross/net exposure or VaR-utilization answer with the basis + SQL shown (M5),
small-cell suppression firing on a counterparty × asset-class × desk cut (M6). Copy a `<figure class="shot">` block to add slots.
