# Screenshot shot list

Drop PNGs into `assets/` with these exact filenames and they appear in the guide automatically.
Capture at ~1280-1440px, light mode, synthetic data only (the demo P&C carrier set — never real policyholder/claimant PII).

| # | File | Module | What to capture |
|---|---|---|---|
| 1 | `assets/ins-m1-git.png` | M1 | The Git connector configured against the starter repo |
| 2 | `assets/ins-m2-pr.png` | M2 | The schema-validation PR Ana opened (schema.tql diff) |

Worth adding: a NAIC-line / peril-group rollup answer (M3), a governed loss-ratio answer with the basis + SQL shown (M4),
fair-pricing small-cell suppression firing on a LOB × state × peril cut (M5). Copy a `<figure class="shot">` block to add slots.
